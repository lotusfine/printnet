services:
  backend:
    build: ./backend
    container_name: glaxara-backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    networks:
      - glaxara-network
    # healthcheck:
    #   test: ["CMD", "curl", "-f", "http://localhost:8080/api/informacion"]
    #   interval: 30s
    #   timeout: 10s
    #   retries: 3
    #   start_period: 30s

networks:
  glaxara-network:
    driver: bridge